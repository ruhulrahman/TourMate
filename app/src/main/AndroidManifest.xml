<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tourmate">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-feature android:name="android.hardware.location.gps"/>
    <uses-feature android:name="android.hardware.location.network"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EditExpenseActivity"
            android:label="Edit Expense"
            android:theme="@style/ActionBarPomegrane" />
        <activity android:name=".activity.DetailsTourActivity" />
        <activity android:name=".activity.AddTourActivity" />
        <activity
            android:name=".activity.ExpenseDetailsActivity"
            android:label="View Expense Details"
            android:theme="@style/ActionBarPomegrane" />
        <activity
            android:name=".activity.ViewExpenseActivity"
            android:label="@string/add_expense"
            android:theme="@style/ActionBarPomegrane" />

        <activity
            android:name=".activity.GoogleMapsActivity"
            android:label="@string/title_activity_google_maps" />

        <activity
            android:name=".activity.AddExpenseActivity"
            android:label="@string/add_expense"
            android:theme="@style/ActionBarPomegrane"></activity>
        <activity
            android:name=".activity.SignUpActivity"
            android:theme="@style/noActionBar" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/noActionBar" />
        <activity
            android:name=".activity.SpashActivity"
            android:theme="@style/noActionBar" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC9DZV98bXVgZEYmV1VJyBMh_mOy7PnLDE" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>